Factory server executable and configuration

    cgi-bin - files that are executable by web server

    root - config utilities that go in root directory

    etc - config files

Must install:

    apache2
    dnsmasq
    iptables
    iptables-persistent    


