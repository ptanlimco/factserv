# phase3 test list example

# This test will pass or fail at random
Final test : 5 : c=$(tr -dc AB < /dev/urandom 2>/dev/null | head -c1); if [ "$c" = A ]; then echo Test randomly failed; exit 1; else echo Test randomly passed; fi
